apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  labels:
    app: apishield-redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  labels:
    app: apishield-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apishield-redis
  template:
    metadata:
      labels:
        app: apishield-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: redis-storage
              mountPath: /data
      volumes:
        - name: redis-storage
          persistentVolumeClaim:
            claimName: redis-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: apishield-redis
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: apishield-redis
  type: ClusterIP
